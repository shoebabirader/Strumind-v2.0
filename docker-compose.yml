version: '3.8'

services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: strumind
      POSTGRES_USER: strumind_user
      POSTGRES_PASSWORD: strumind_pass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: postgresql://strumind_user:strumind_pass@postgres:5432/strumind
      JWT_SECRET: your-secret-key-change-in-production
    depends_on:
      - postgres
    volumes:
      - ./backend:/app

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: http://localhost:8000
    depends_on:
      - backend
    volumes:
      - ./frontend:/app
      - /app/node_modules

volumes:
  postgres_data:
